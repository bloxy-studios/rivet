{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "bunVersion": "1.4.x",
  "installCommand": "cd ../.. && curl -fsSL https://bun.sh/install | bash -s bun-v1.4.0 && ~/.bun/bin/bun install --frozen-lockfile",
  "buildCommand": "cd ../.. && PATH=$HOME/.bun/bin:$PATH bunx turbo run build --filter=server",
  "rewrites": [{ "source": "/:path*", "destination": "/api/server" }]
}
